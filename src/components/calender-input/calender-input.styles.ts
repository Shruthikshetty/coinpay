import {StyleSheet} from 'react-native';

const calenderInputStyles = StyleSheet.create({
  inputPress: {
    ...StyleSheet.absoluteFillObject,
    zIndex: 10,
    borderRadius: 10,
  },
});

export default calenderInputStyles;
