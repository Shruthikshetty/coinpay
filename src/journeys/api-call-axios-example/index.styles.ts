import {StyleSheet} from 'react-native';

const exampleApiScreenStyles = StyleSheet.create({
  title: {
    textAlign: 'left',
    fontWeight: '500',
    fontSize: 15,
    marginBottom: 3,
  },
  header: {
    fontSize: 28,
    marginBottom: 10,
    textAlign: 'center',
    fontWeight: 'bold',
  },
});

export default exampleApiScreenStyles;
