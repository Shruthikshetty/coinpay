import {StyleSheet} from 'react-native';
import {colors} from '~/common/constants/colors.constants';

const scree1Styles = StyleSheet.create({
  mainContainer: {
    flex: 1,
    backgroundColor: colors.gray100,
  },
  heading: {
    fontSize: 30,
    fontWeight: 'bold',
    alignSelf: 'center',
    textAlign: 'center',
  },
});

export default scree1Styles;
